import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Example2-23Menu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
